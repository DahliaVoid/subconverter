diff --git a/src/generator/config/subexport.cpp b/src/generator/config/subexport.cpp
index 36f6269..c7d33a2 100644
--- a/src/generator/config/subexport.cpp
+++ b/src/generator/config/subexport.cpp
@@ -686,8 +686,6 @@ void proxyToClash(std::vector<Proxy> &nodes, YAML::Node &yamlnode, const ProxyGr
                     break;
             }
 
-            if (!x.Fingerprint.empty())
-                singleproxy["fingerprint"] = x.Fingerprint;
             if (x.XTLS == 2) {
                 singleproxy["flow"] = "xtls-rprx-vision";
             } else if (!x.FlowSet.is_undef() && x.FlowSet.get()) {
@@ -700,6 +698,8 @@ void proxyToClash(std::vector<Proxy> &nodes, YAML::Node &yamlnode, const ProxyGr
             if (!x.PublicKey.empty() && !x.ShortID.empty()) {
                 singleproxy["reality-opts"]["public-key"] = x.PublicKey;
                 singleproxy["reality-opts"]["short-id"] = x.ShortID;
+                if (!x.SpiderX.empty())
+                    singleproxy["reality-opts"]["spider-x"] = x.SpiderX;
                 if (!x.ClientFingerprint.empty()) {
                     singleproxy["client-fingerprint"] = x.ClientFingerprint;
                 } else if (!x.Fingerprint.empty()) {
diff --git a/src/parser/config/proxy.h b/src/parser/config/proxy.h
index 62d95cd..2931397 100644
--- a/src/parser/config/proxy.h
+++ b/src/parser/config/proxy.h
@@ -177,6 +177,7 @@ struct Proxy
     tribool AuthenticatedLength;
     tribool XUDP;
     String ShortID;
+    String SpiderX;                    // reality-opts spider-x
 
     // New parameters from mihomo
     String IpVersion;                    // ip-version: ipv4/ipv6/dual/ipv4-prefer/ipv6-prefer
diff --git a/src/parser/subparser.cpp b/src/parser/subparser.cpp
index 6ef0f7f..02f4e9b 100644
--- a/src/parser/subparser.cpp
+++ b/src/parser/subparser.cpp
@@ -377,7 +377,9 @@ void vlessConstruct(
             break;
     }
     if (!alpn.empty()) {
-        node.Alpn = StringArray{alpn};
+        node.Alpn = split(alpn, ",");
+        for (auto &item : node.Alpn)
+            item = trim(item);
     }
     node.Fingerprint = fingerprint;
     node.Flow = flow;
@@ -1246,7 +1248,7 @@ void explodeClash(Node yamlnode, std::vector<Proxy> &nodes)
         std::string obfs_password, cwnd; //hysteria2
         std::string uuid, heartbeat_interval, disable_sni, reduce_rtt, request_timeout, udp_relay_mode, congestion_controller, max_udp_relay_packet_size, max_open_streams, fast_open;   //TUIC
         std::string idle_session_check_interval, idle_session_timeout, min_idle_session;
-        std::string flow, xtls, short_id;
+        std::string flow, xtls, short_id, spider_x;
         // New parameters from mihomo
         std::string ip_version, client_fingerprint, ech_config, certificate, private_key_pem, vless_encryption;
         std::string smux_protocol, ws_early_data_header_name, http_method, trojan_ss_method, trojan_ss_password;
@@ -1354,10 +1356,12 @@ void explodeClash(Node yamlnode, std::vector<Proxy> &nodes)
             singleproxy["uuid"] >>= uuid;
             singleproxy["servername"] >>= sni;
             net = singleproxy["network"].IsDefined() ? safe_as<std::string>(singleproxy["network"]) : "tcp";
-            if (singleproxy["alpn"].IsSequence())
-                singleproxy["alpn"][0] >>= alpn;
-            else
+            if (singleproxy["alpn"].IsSequence()) {
+                for (const auto &alpn_item : singleproxy["alpn"])
+                    node.Alpn.emplace_back(safe_as<std::string>(alpn_item));
+            } else {
                 singleproxy["alpn"] >>= alpn;
+            }
             singleproxy["fingerprint"] >>= fingerprint;
             singleproxy["flow"] >>= flow;
             if (singleproxy["flow"].IsDefined())
@@ -1365,6 +1369,7 @@ void explodeClash(Node yamlnode, std::vector<Proxy> &nodes)
             if (singleproxy["reality-opts"].IsDefined()) {
                 singleproxy["reality-opts"]["public-key"] >>= public_key;
                 singleproxy["reality-opts"]["short-id"] >>= short_id;
+                singleproxy["reality-opts"]["spider-x"] >>= spider_x;
             }
             switch (hash_(net))
             {
@@ -1410,6 +1415,7 @@ void explodeClash(Node yamlnode, std::vector<Proxy> &nodes)
 
             vlessConstruct(node, group, ps, server, port, uuid, sni, alpn, type, net, mode, host, path, fingerprint, flow, xtls, public_key, short_id, client_fingerprint, udp, tfo, scv, underlying_proxy);
             node.TLSSecure = tls == "tls";
+            node.SpiderX = spider_x;
             // Assign new parameters to node for VLESS
             node.IpVersion = ip_version;
             node.EchEnable = ech_enable;
@@ -2184,7 +2190,11 @@ void explodeStdVLESS(std::string vless, Proxy &node) {
         }
         net = getUrlArg(addition,"type");
         alpn = getUrlArg(addition, "alpn");
-        fingerprint = getUrlArg(addition, "hpkp");
+        fingerprint = getUrlArg(addition, "fp");
+        if (fingerprint.empty())
+            fingerprint = getUrlArg(addition, "fingerprint");
+        if (fingerprint.empty())
+            fingerprint = urlDecode(getUrlArg(addition, "hpkp"));
         flow = getUrlArg(addition, "flow");
         xtls = getUrlArg(addition, "xtls");
         public_key = getUrlArg(addition, "pbk");
@@ -2230,6 +2240,7 @@ void explodeStdVLESS(std::string vless, Proxy &node) {
         remarks = add + ":" + port;
     node.TLSSecure = security == "tls" || security == "reality";
     vlessConstruct(node, VLESS_DEFAULT_GROUP, remarks, add, port, uuid, sni, alpn, type, net, mode, host, path, fingerprint, flow, xtls, public_key, short_id, "", tribool(), tfo, scv, "");
+    node.SpiderX = urlDecode(getUrlArg(addition, "spx"));
 }
 
 void explodeVLESS(std::string vless, Proxy &node) {
